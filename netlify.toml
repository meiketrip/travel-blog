[build]
command = "hugo --gc --minify"
publish_directory = "public"

[build.environment]
HUGO_VERSION = "0.146.0-extended"

# 关键：跳过npm依赖安装，解决Install dependencies报错
[build.processing]
skip_package_dependencies = true